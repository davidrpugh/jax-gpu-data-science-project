name: null

channels:
  - conda-forge
  - defaults

dependencies:
  - cudatoolkit=11.0
  - cudnn=8.0
  - jupyterlab<3.0
  - jupyterlab-git
  - matplotlib
  - pandas
  - pip
  - pip:
    - -r file:requirements.txt
  - python
  
